import type { Project } from '../types'

export const projects: Project[] = [
  {
    id: 'social-saas',
    title: 'Kaizex — Marketing Operating System',
    shortDescription:
      'Multi-tenant content pipeline: Source → AI Knowledge Extraction → Strategy-Driven Generation → Scheduled Distribution. BYO-AI (5 providers), three automation modes, Redis time-slot scheduling, three async RabbitMQ pipelines, Elasticsearch-backed post and knowledge stores.',
    description:
      'SMAT (Kaizex backend) is a Spring Boot multi-module system that automates the full content lifecycle for marketing teams. Organizations connect social channels via PKCE OAuth2 (LinkedIn live, X wired, Instagram planned), configure a content strategy per channel (audience, pillars, tone, automation mode, posting cadence), and add knowledge sources — URLs or raw text — into theme-based Buckets. Three independent RabbitMQ pipelines handle content processing (knowledge extraction from sources), content generation (AI draft creation from strategy + knowledge), and post dispatch (platform publishing). Generated posts flow through a status lifecycle: DRAFT → PENDING_REVIEW → SCHEDULED → IN_PROGRESS → PUBLISHED/FAILED. A Redis time-slot engine (24 hourly slots per day, 7-day TTL) manages scheduling; four crons handle slot hydration, dispatch, cleanup, and strategy-triggered generation. BYO-AI supports five provider adapters: OpenAI/OpenRouter, Anthropic, Google Gemini, Ollama (local), and a fully configurable Custom endpoint with dot-path response extraction. Multi-tenant RBAC is enforced at method level via @PreAuthorize, with 15 default permissions seeded per org on creation.',
    techStack: ['Java', 'Spring Boot', 'Next.js', 'TypeScript', 'RabbitMQ', 'Redis', 'Elasticsearch', 'MariaDB', 'Liquibase', 'Spring Cloud OpenFeign', 'Docker', 'Nginx'],
    status: 'LIVE',
    liveUrl: '/apps',
    architecture: [
      'Module layout — smat-api-runner (entry point, controllers, security filter chain), smat-service (business logic), smat-entity (JPA entities, Elasticsearch docs), smat-common (shared DTOs, enums), smat-consumer (RabbitMQ listeners), smat-automation (cron jobs), smat-cron-runner (cron bootstrap).',
      'Three RabbitMQ pipelines — content-processing-queue (source URL/text → Jsoup extraction → AI knowledge → Elasticsearch processed_content_index), content-generation-queue (strategy → prompt from knowledge context → AI generation → route by automation mode), post-processing-queue (task ID → platform handler → LinkedIn UGC post / X stub / Instagram stub).',
      'Redis time-slot scheduling — 24 hourly list keys per day (TTL 7d). Midnight cron hydrates all PENDING/IN_PROGRESS tasks into slots (3-thread pool). Top-of-hour cron LPOP dispatches due task IDs to post-processing-queue. 5-min-past-hour cron fails and clears any tasks left in the previous slot.',
      'BYO-AI via Spring Cloud OpenFeign — five provider adapters: OpenAI/OpenRouter (/v1/chat/completions), Anthropic (messages API), Google Gemini (generateContent), Ollama (local /api/generate), Custom (configurable URL + payload template + dot-path JSON response extractor). One active config per org.',
      'Social channel OAuth2 PKCE — LinkedIn (token exchange + UGC post publish fully live), X (token exchange + createTweet/removeTweet implemented; dispatch handler stubbed pending approval), Instagram (auth scaffolded). Anti-replay state via Redis TTL-bound keys consumed on callback.',
      'Multi-tenant RBAC — 15 AppPermission entries seeded on org creation, custom permissions + roles per org, @PreAuthorize on every endpoint. 15 MariaDB tables via Liquibase changelogs. Three Elasticsearch indices: posts, processed_content_index, instagram_analytics.',
    ],
    features: [
      'Three independent async RabbitMQ pipelines: knowledge extraction, AI generation, platform dispatch — each failure-isolated and independently scalable',
      'Five AI provider adapters: OpenAI/OpenRouter, Anthropic, Google Gemini, Ollama (local), Custom endpoint — zero model lock-in, one active config per org',
      'Three automation modes per channel: MANUAL (always draft), REVIEW (human approval before scheduling), AUTOPILOT (auto-schedule on generation)',
      'Redis time-slot engine: 24 hourly buckets per day, midnight hydration, top-of-hour dispatch, 5-min slot cleanup — reliable scheduling without a heavyweight queue',
      'Content Knowledge Pipeline: add URLs or raw text as Sources → async AI knowledge extraction → Elasticsearch → context injected into every generation prompt',
      'OAuth2 PKCE for LinkedIn (fully live), X (auth + tweet API wired), Instagram (auth scaffolded) — extensible connector architecture',
      'Method-level RBAC via @PreAuthorize, 15 default permissions auto-seeded per org, full custom role and permission management',
      'Elasticsearch post store with 8-dimension dynamic search: channel, platform, status, source, content pillar, hashtag, and date range',
    ],
    year: 2025,
  },
  {
    id: 'microservices-migration',
    title: 'Monolith to Microservices Migration',
    shortDescription:
      'Led domain decomposition and phased service extraction at Rampwin. Reduced MTTR by 28%, eliminated cross-domain cascading failures, and enabled independent team deployments.',
    description:
      'At Rampwin, the monolith had become the org-level risk: deployment coupling meant any release could affect unrelated domains, a single bad query could degrade the entire platform, and a service failure could cascade everywhere. I led the migration. Started with event storming to map domain boundaries — no code changes until the boundaries were agreed and service contracts were defined. Used strangler fig for incremental extraction with feature-flag traffic routing. Built the observability layer in parallel so failure modes were tracked from day one, not discovered reactively. Result: MTTR dropped 28%, deployment frequency increased across teams, and cross-domain incidents became structurally impossible.',
    techStack: ['Java', 'Spring Boot', 'MongoDB', 'Kafka', 'Docker', 'Kubernetes', 'Istio'],
    status: 'INTERNAL',
    architecture: [
      'Event storming workshop output: 6 bounded contexts identified and agreed before any code was touched — domain boundaries as first-class design artifacts',
      'Strangler fig extraction: new service traffic routed via feature flags at the Nginx layer; monolith kept live and functional until consumer migration was verified complete',
      'Kafka event bus as the inter-service communication fabric — async, decoupled, replay-capable; services produce and consume domain events without direct coupling',
      'Distributed sagas for cross-service transactions (checkout, order lifecycle) — replacing monolith ACID guarantees with explicit eventual consistency and compensating actions',
      'Per-service MongoDB collections with no shared schema — data ownership enforced at the persistence layer, not just the API layer',
      'Istio service mesh for mTLS between services, distributed tracing via Jaeger, and controlled traffic shaping during phased cutover',
    ],
    features: [
      'MTTR reduced 28% — failures in one bounded context are now structurally contained and cannot cascade',
      'Independent deployment pipelines per service: teams release without coordination overhead or shared deployment windows',
      'API contract testing (Pact) enforced in CI — breaking changes caught before staging, on every commit',
      'Distributed saga orchestration for checkout and order workflows with full compensating transaction support and idempotent retry',
      'Per-service observability from day one: structured logs, distributed traces, and Prometheus metrics built into the service template',
      'MongoDB performance optimization under high-write load: compound index design, write concern tuning, and read replica routing',
    ],
    year: 2025,
  },
  {
    id: 'api-performance',
    title: 'API Performance Overhaul',
    shortDescription:
      'Systematic data-access and caching redesign at CarDekho: p99 DB latency -40%, API latency -35%, user conversion +12%.',
    description:
      'High-traffic API endpoints were degrading under load — p99 DB latency above 820ms, visible user drop-off at key conversion flows. I did a full diagnostic before touching any code: profiled query execution plans, mapped all N+1 patterns, audited cache TTL strategy, and identified three synchronous downstream calls that belonged off the critical path. The fix was systematic: rewrote the data-access layer with explicit batch-fetch queries, added composite covering indexes on the hot query patterns, replaced the self-defeating fine-grained cache TTLs with domain-aligned invalidation, and moved downstream enrichment to async CompletableFuture chains returning after the core response. Outcome: p99 DB latency dropped 40%, API latency dropped 35%, user conversion at the affected flow improved 12%.',
    techStack: ['Java', 'Spring Boot', 'MySQL', 'Redis', 'Prometheus', 'Grafana'],
    status: 'INTERNAL',
    architecture: [
      'JPA query audit: replaced ORM-generated N+1 patterns with explicit JPQL batch-fetch queries — DB round trips on the critical path reduced 6x',
      'Composite index design on MySQL: EXPLAIN-driven analysis of the top-10 hot query patterns; covering indexes added to eliminate full-table scans at load',
      'Redis caching redesign: domain-aligned TTLs, tag-based invalidation on write mutations, probabilistic early expiration to prevent cache stampede under burst traffic',
      'Async enrichment: downstream service calls moved off the critical path using CompletableFuture; core response returned immediately, enrichments applied without blocking the caller',
      'Prometheus latency histograms (p50/p95/p99) per endpoint added as deployment-gate signals in CI — regressions caught before production',
    ],
    features: [
      'p99 DB latency: 820ms to 490ms (-40%)',
      'API p99 latency: 1050ms to 680ms (-35%)',
      'User conversion +12% at the checkout and listing flow directly attributable to latency improvement',
      'Redis cache hit rate improved from 54% to 87% via TTL and invalidation strategy redesign',
      'Zero latency SLO regressions across two quarters post-deployment',
    ],
    year: 2023,
  },
  {
    id: 'cicd-automation',
    title: 'CI/CD Automation and Deployment Reliability',
    shortDescription:
      'Designed canary pipelines, automated rollback on Prometheus burn-rate triggers, and contract testing gates at Equinix. Deployment failure rate -30%.',
    description:
      'Deployment failures compound their damage: they pull engineers off feature work, erode team confidence in the release process, and inflate incident MTTR. At Equinix, I redesigned the deployment pipeline end-to-end: canary releases with traffic ramp gates based on live Prometheus error rate and p99 latency signals, automated rollback via Kubernetes rollout undo on burn-rate threshold breach, Pact contract testing in CI catching API breaking changes before staging, and Helm chart standardisation reducing pipeline onboarding to under 30 minutes per new service. Deployment failure rate dropped 30%, mean rollback time dropped from 12 minutes manual to under 60 seconds automated.',
    techStack: ['Jenkins', 'Kubernetes', 'Helm', 'Prometheus', 'Java', 'Spring Boot', 'Docker'],
    status: 'LIVE',
    architecture: [
      'Canary deployment strategy: 5% to 25% to 100% traffic ramp with automated hold gates based on HTTP error rate and p99 latency thresholds at each stage',
      'Automated rollback: Prometheus alerting rule firing on canary error budget burn triggers Kubernetes rollout undo via webhook — rollback completes in under 60 seconds without human intervention',
      'Contract testing (Pact) in CI pipeline — provider and consumer sides tested on every commit; breaking changes block merge before staging is ever touched',
      'Helm chart templating with per-environment value overlays and sealed secret management — configuration parity between staging and production enforced at the template layer',
      'Jenkins shared library encapsulating the full build-test-scan-deploy sequence — service onboarding requires only a Jenkinsfile referencing the shared library',
    ],
    features: [
      'Deployment failure rate -30% via canary gates and automated rollback',
      'Mean rollback time: 12 minutes manually to under 60 seconds automated via Prometheus-triggered Kubernetes webhook',
      'API breaking changes caught in CI before staging — 100% contract test gate coverage across 5 active service pairs',
      'Standardised deployment pipeline adopted across 5+ services; new service onboarding reduced to under 30 minutes',
      'Runbook library covering 12 failure modes — on-call MTTR improvement measurable within 2 sprint cycles',
    ],
    year: 2025,
  },
  {
    id: 'slo-observability',
    title: 'SLO-Based Observability Platform',
    shortDescription:
      'Shifted the team from reactive alerting to error budget engineering. SLOs defined per service, multi-window burn-rate alerting implemented, on-call noise reduced significantly.',
    description:
      'Most alerting systems page on symptoms — CPU spikes, memory pressure, error rate above threshold. The signal arrives too late and carries no context about actual service-level impact. I built an SLO-based observability layer: defined availability and latency SLOs per service, implemented multi-window burn-rate alerting (Google SRE Book Chapter 5 approach), and built Grafana dashboards showing error budget consumption in real time. The operational shift: on-call is paged only when burn rate indicates a genuine threat to SLO compliance within the measurement window — not on every transient spike. Alert noise dropped significantly, the team started treating error budgets as an engineering constraint rather than a vanity metric.',
    techStack: ['Prometheus', 'Grafana', 'ELK Stack', 'Java', 'Spring Boot', 'Kubernetes', 'PagerDuty'],
    status: 'INTERNAL',
    architecture: [
      'SLO definitions as code: availability (99.9% request success rate) and latency (p99 < 300ms) targets per service in version-controlled YAML, reviewed and signed off like any service contract',
      'Prometheus recording rules pre-computing burn rate over 1h, 6h, and 3d windows — fast-burn and slow-burn evaluation without query overhead at alert evaluation time',
      'Multi-window alerting: fast-burn (1h and 6h windows) triggers PagerDuty; slow-burn (3d) routes to Slack — alert noise reduced by removing single-spike false positives',
      'Grafana error budget dashboards: remaining budget percentage, projected exhaustion time, and per-endpoint SLO heatmap updated in real time',
      'ELK pipeline: Spring Boot JSON structured logs routed through Logstash to Elasticsearch; Kibana used for incident investigation with full correlation ID and trace ID join',
    ],
    features: [
      'SLO coverage across 8 production services with per-service error budget tracking and quarterly compliance reporting',
      'On-call alert noise reduced — multi-window burn rate eliminates single-spike false positives that previously triggered unnecessary pages',
      'Error budget as an engineering constraint: feature freeze and reliability work triggers defined by budget consumption, not subjective team decision',
      'Structured logging standard adopted org-wide: correlation IDs, trace IDs, and request context on every log line',
      'Automatic post-incident timeline generation from Kibana log queries and Jaeger trace spans — RCA time per incident reduced measurably',
    ],
    year: 2024,
  },
  {
    id: 'session-logger',
    title: 'Session Logger — Freelance Time Tracker',
    shortDescription:
      'A full-stack freelance session tracker that replaces spreadsheets and stopwatches with one-click time tracking, automatic billing calculation, and exportable monthly reports.',
    description:
      'Freelancers and consultants who bill clients hourly typically juggle a stopwatch, a notes app, and a spreadsheet. Session Logger replaces that entire workflow with a single self-hosted web app. One-click session start with a live elapsed timer, concurrent-session guard (409 on duplicates), billing snapshots that freeze the rate at session start so future rate changes never corrupt historical invoices, monthly billing breakdowns with CSV export, and a filterable session history. The timer initialises from the real start timestamp on every page load — a browser refresh never resets the clock. The entire stack runs on a single VPS with zero external SaaS dependencies.',
    techStack: ['React', 'TypeScript', 'Vite', 'Tailwind CSS', 'React Query', 'Zustand', 'Axios', 'Node.js', 'Express', 'Prisma', 'PostgreSQL', 'Zod', 'JWT', 'Docker', 'Nginx'],
    status: 'LIVE',
    liveUrl: 'https://buildwithkulshresth.com/session-logger',
    githubUrl: 'https://github.com/KulshresthJangid/session-logger',
    architecture: [
      'Frontend: React 18 + TypeScript + Vite 5. React Query v5 as the single source of truth for all server state; Zustand only for auth token management. Tailwind CSS dark-only design.',
      'Backend: Node.js + Express + TypeScript. Zod schema-first validation on every request body and query parameter. JWT auth (jsonwebtoken + bcryptjs).',
      'Database: Prisma v5 ORM over PostgreSQL 16 in Docker Compose. Billing snapshot pattern — client rate copied into session row at start time.',
      'Timer architecture: useTimer hook initialises elapsed seconds in useState lazy initialiser (Date.now() - startTime), not in useEffect — no "00:00:00" flash on page load or refresh.',
      'Concurrent session guard: API returns 409 Conflict if a second session is started for the same client. Server-side duration and cost computation on session close.',
      'Infra: single VPS, Docker Compose for Postgres, nohup process for the Node server, nginx reverse proxy. ./start.sh builds frontend + backend, runs migrations, and starts the server.',
    ],
    features: [
      'Dashboard with real-time stat cards: total clients, sessions this month, revenue this month, currently active sessions — auto-refreshes every 10 seconds',
      'One-click session start/stop with live HH:MM:SS elapsed timer initialised from real start timestamp on every page load',
      'Billing snapshot: client rate frozen at session start — rate changes never retroactively alter past invoices',
      'Concurrent session guard: 409 Conflict on duplicate session start for the same client',
      'Monthly billing reports: filter by month (up to 12 months back) and by client, with per-client summary and grand total row',
      'CSV export for dropping straight into an invoice template or accountancy tool',
      'Full session history: filterable by client, status (ACTIVE / COMPLETED / ABANDONED), and date range',
      'Client CRUD with per-client hourly/fixed rate, optional monthly budget, tags, and notes',
      'Secure auth: bcrypt + JWT, 401 auto-redirect, token stored in localStorage with Axios interceptor',
      'Self-hosted, zero external SaaS dependencies — entire stack on a single VPS',
    ],
    year: 2025,
  },
  {
    id: 'sso-platform',
    title: 'Enterprise SSO Security Portal',
    shortDescription:
      'Multi-tenant OpenID Connect (OIDC) Authorization Server with centralized single sign-on, RBAC admin dashboard, and tenant isolation — 100% password compliance across all connected corporate systems, employee provisioning time cut by 50%.',
    description:
      'Growing organizations face real security risk when employees maintain separate passwords across every internal and external application. This breeds credential vulnerabilities, inflates help-desk ticket volume, and makes new-hire provisioning a multi-hour manual task. I built a centralized, multi-tenant authentication server fully compliant with the OpenID Connect (OIDC) standard. Every corporate application is secured under a single sign-on portal, with role-based permission settings managed from a master administrator dashboard. Tenant isolation is enforced at the data and routing layer — each organization\'s client configurations, registered applications, and user roles are completely segregated. The authorization server issues signed JWTs with custom claims, supports PKCE for SPA clients, and exposes a compliance-ready token introspection endpoint. The admin dashboard enables non-technical administrators to provision users, assign roles, and register new OAuth2 client applications without touching config files.',
    techStack: ['Java', 'Spring Boot', 'Spring Authorization Server', 'OIDC', 'OAuth2', 'React', 'TypeScript', 'Vite', 'PostgreSQL', 'Flyway', 'JWT', 'Docker', 'Nginx'],
    status: 'LIVE',
    liveUrl: 'https://buildwithkulshresth.com/sso',
    architecture: [
      'Spring Authorization Server (Spring Security 6): three security filter chains — one for the authorization server endpoints (.well-known/openid-configuration, /oauth2/authorize, /oauth2/token), one for the admin dashboard, one for the API layer. All chains independently scoped.',
      'Multi-tenant isolation: TenantResolutionFilter extracts org slug from the request path or subdomain, loads the correct tenant context, and scopes all downstream queries — client lookups, user store, role assignments — to that tenant with no cross-tenant data bleed.',
      'Custom token customizer (SSOTokenCustomizer): injects org ID, tenant slug, and RBAC role claims into every issued JWT. Downstream services consume these claims for authorization without a separate lookup.',
      'Persistent client registry (TenantAwareRegisteredClientRepository): OAuth2 registered clients stored in PostgreSQL per tenant, managed via admin API. Supports confidential clients (server-side apps) and public clients (SPAs) with PKCE enforcement.',
      'Admin frontend (React + Vite + Tailwind): SPA with protected routes — AppsPage (register/revoke OAuth2 clients), UsersPage (create/deactivate users), RolesPage (define and assign roles), OverviewPage (active session stats). Auth via OIDC callback into the same authorization server it manages.',
      'Database migrations via Flyway: all schema changes version-controlled and applied on startup. OAuth2 authorization, consent, and token tables follow Spring Authorization Server schema conventions.',
    ],
    features: [
      '100% password compliance across all connected corporate systems — single credential, zero per-app password sprawl',
      'Employee account provisioning and onboarding time reduced by 50% via admin dashboard user creation and role assignment',
      'OIDC-compliant .well-known/openid-configuration endpoint — any standards-compliant client integrates in minutes',
      'Multi-tenant isolation: each organization\'s clients, users, and roles are fully segregated at the database layer',
      'PKCE enforcement for SPA clients — eliminates authorization code interception attacks on public clients',
      'Custom JWT claims (org ID, tenant slug, roles) — downstream services enforce RBAC without a separate user-info lookup',
      'Admin dashboard: register and revoke OAuth2 applications, manage users and roles, view active session stats — no config file editing required',
      'Credential-stuffing and unauthorized access risk eliminated via centralized, audited authentication path',
    ],
    year: 2025,
  },
  {
    id: 'goalbegins',
    title: 'GoalBegins — Corporate Services Portal',
    shortDescription:
      'High-performance corporate website for a Jaipur IT services enterprise — 98+ Google Lighthouse score, interactive booking flows, real-time repair ticket tracking, and self-service customer status portal.',
    description:
      'A Jaipur-based IT services enterprise needed a modern, high-performance web presence that could do three jobs at once: showcase services to new visitors, capture inbound leads through frictionless booking, and let existing customers track their repair or service request status without calling support. I architected and deployed a fully responsive corporate website with server-side rendering for optimal search engine indexing. The booking flow was designed to minimize form friction — service selection, contact capture, and confirmation in three steps. The ticket tracking portal lets customers self-serve status lookups using a reference number, reducing inbound support volume. Smooth page transitions and consistent animation give the site the polished feel expected from a professional IT firm.',
    techStack: ['Next.js', 'React', 'TypeScript', 'Tailwind CSS', 'Node.js', 'PostgreSQL', 'Docker', 'Nginx'],
    status: 'LIVE',
    liveUrl: 'https://goalbegins.com',
    architecture: [
      'Next.js App Router with server-side rendering on all public pages — HTML delivered pre-rendered for maximum Lighthouse performance and SEO score. Static generation for service catalog pages.',
      'Interactive booking form: multi-step wizard component with client-side validation, optimistic UI, and server-action submission. Booking records persisted to PostgreSQL; confirmation email triggered on success.',
      'Ticket tracking portal: customers enter a reference number and receive real-time status pulled from the service database — eliminates the majority of inbound "where is my repair?" calls.',
      'Nginx reverse proxy with Brotli compression, aggressive cache headers on static assets, and HTTP/2 — contributes directly to the 98+ Lighthouse performance score.',
      'Fluid page transitions using Next.js route-level animations — consistent motion design across all pages without sacrificing CLS scores.',
    ],
    features: [
      '98+ Google Lighthouse Performance and SEO score — maximizes organic discoverability and first-impression loading speed',
      'Frictionless three-step booking flow: service selection, contact capture, confirmation — designed to minimize abandonment and maximize lead conversion',
      'Self-service ticket tracking portal: customers look up repair status by reference number, reducing inbound support call volume',
      'Server-side rendering for all public pages — content indexed by search engines on first crawl, no JS execution required',
      'Smooth route-level page transitions with zero Cumulative Layout Shift (CLS) impact',
      'Fully responsive across mobile, tablet, and desktop — consistent brand experience regardless of device',
    ],
    year: 2024,
  },
  {
    id: 'webhook-proxy',
    title: 'Secure Webhook Proxy & Relay Gateway',
    shortDescription:
      'A centralized webhook relay server that captures, logs, and forwards Stripe/Meta/WhatsApp webhook payloads to local development environments — zero local network exposure, 2× faster integration testing.',
    description:
      'Integrating payment channels (Stripe) or messaging networks (Meta, WhatsApp) requires receiving live event notifications during development and testing. The naive approach — exposing a local port via ngrok or SSH tunnel — creates a direct public internet path into the developer\'s machine and introduces a live credential security risk. I built a centralized, secure webhook proxy server that eliminates this exposure entirely. The relay server receives all inbound webhook calls at a stable public HTTPS endpoint, handles vendor-specific verification handshakes (HMAC signature validation for Stripe, challenge-response for Meta), persists every payload to a database audit log, and forwards the event to the registered local target. Developers replay any logged event with a single click — eliminating the "trigger the event again" cycle that burns hours during integration debugging.',
    techStack: ['Node.js', 'Express', 'React', 'Vite', 'Tailwind CSS', 'MongoDB', 'JWT', 'Docker', 'Nginx'],
    status: 'LIVE',
    liveUrl: 'https://buildwithkulshresth.com/webhook-proxy',
    githubUrl: 'https://github.com/KulshresthJangid/webhook-proxy',
    architecture: [
      'Express relay server: single public HTTPS endpoint receives all inbound webhook calls. Route-level handler resolves the target registration from MongoDB, validates the vendor HMAC signature or challenge-response, and forwards the payload to the registered local URL via Axios.',
      'Vendor verification layer: Stripe webhooks validated via stripe-signature header (timing-safe HMAC comparison). Meta/WhatsApp hub.verify challenge-response handled at the GET endpoint before any POST payload is accepted.',
      'MongoDB payload store: every inbound request persisted with timestamp, source vendor, HTTP headers, raw body, forwarding target, response status, and latency. Enables full audit trail and replay without re-triggering the external vendor.',
      'One-click replay API: POST /replay/:eventId re-sends the stored raw payload to the registered target — eliminates the manual "go back to Stripe dashboard and trigger again" cycle during debugging.',
      'React dashboard (Vite + Tailwind): live event log with filter by vendor, status, and date. Per-event detail view showing raw headers, body, and forwarding response. Replay button inline.',
      'JWT-authenticated admin API — all relay registrations and event log access require a valid token. Nginx reverse proxy terminates TLS.',
    ],
    features: [
      '100% secure API testing: zero local network vulnerability — local dev environment never exposed to the public internet',
      'Vendor HMAC signature validation (Stripe) and challenge-response handling (Meta/WhatsApp) — relay server speaks each vendor\'s verification protocol natively',
      'Durable audit log: every inbound payload stored with headers, body, forwarding status, and latency — full debugging trail across sessions',
      'One-click event replay: re-send any historical payload to the target with a single API call — eliminates the "trigger the webhook again" debugging cycle',
      'Integration development cycle speed increased 2× — replay replaces the multi-step manual re-trigger flow',
      'React dashboard with live event log: filter by vendor, status, and date; inspect raw headers and body per event',
      'Multi-vendor architecture: designed to add new webhook sources (GitHub, Shopify, Twilio) without changes to the relay core',
    ],
    year: 2025,
  },
  {
    id: 'leadgen-pro',
    title: 'LeadGen Pro — AI-Powered B2B Lead Generation Engine',
    shortDescription:
      'A fully self-hosted B2B lead generation platform that scrapes public sources, deduplicates contacts, and uses a local AI (Mistral 7B via Ollama) to qualify, score, and write personalised outreach copy — zero recurring AI cost.',
    description:
      'LeadGen Pro is a self-hosted B2B lead generation engine designed for zero marginal cost at any volume. It continuously scrapes GitHub, GitLab, HackerNews "Who wants to be hired?", Google Custom Search, and custom URLs using 80+ built-in query templates across India, USA, UK, Europe, APAC, LATAM, and the Middle East. Each lead is deduplicated via SHA-256 email fingerprinting and Levenshtein fuzzy matching on name + domain, then enriched by Mistral 7B running locally on a GTX 1050 GPU via CUDA — identifying pain points, scoring quality (hot/warm/cold), assigning a confidence score (0–100), and generating hyper-personalised outreach copy referencing the lead\'s specific role and company. A daily target mode runs the pipeline continuously until your quota is hit, auto-pauses until midnight, and resumes. An hourly sweep catches any leads Ollama missed during the main run. The entire stack is production-hardened: JWT-authenticated API, rate limiting on all endpoints, CentOS Stream 9, Nginx reverse proxy, PM2 process manager, and DKMS NVIDIA driver signed for Secure Boot.',
    techStack: ['Node.js', 'Express.js', 'React', 'Vite', 'Tailwind CSS', 'TanStack Query', 'Socket.io', 'Mistral 7B', 'Ollama', 'NVIDIA CUDA', 'SQLite', 'PostgreSQL', 'Axios', 'Cheerio', 'JWT', 'Nginx', 'PM2', 'Docker', 'CentOS Stream 9'],
    status: 'LIVE',
    liveUrl: 'https://buildwithkulshresth.com/drip',
    architecture: [
      'Frontend: React 18 + Vite + TailwindCSS + TanStack Query. Real-time dashboard with live WebSocket updates via Socket.io. Filter by quality, source, status, date, location. Manual category override and per-lead notes.',
      'Backend: Node.js 20 (ESM) + Express.js. JWT HS256 auth, rate limiting on all endpoints. REST API for lead management, scraping control, AI enrichment triggers, and CSV export.',
      'AI Layer: Mistral 7B running locally via Ollama on GTX 1050 GPU (CUDA). Two-pass enrichment — first pass: pain point identification, quality scoring (hot/warm/cold), confidence score (0–100). Second pass: hyper-personalised outreach copy guided by ICP + product description from settings.',
      'Scraping Engine: Axios + Cheerio for HTML sources, GitHub API, Algolia HN API, Google Custom Search Engine. Rate-limited with randomised user-agents and per-domain throttling. 80+ built-in GitHub query templates covering global regions.',
      'Deduplication: SHA-256 email fingerprinting for exact match, Levenshtein fuzzy match on name + domain to catch variations. Runs before AI enrichment to avoid wasting GPU cycles.',
      'Pipeline Orchestration: Daily target mode (e.g. 500/day) runs back-to-back scrape → dedup → enrich cycles with zero idle time. Auto-pauses on quota hit, resumes at midnight. Hourly sweep catches leads missed during main run.',
      'Infra: CentOS Stream 9, Nginx reverse proxy, PM2 process manager. DKMS NVIDIA driver signed for Secure Boot. SQLite (zero-config) or PostgreSQL — swappable via env var.',
    ],
    features: [
      'Automated multi-source scraping: GitHub, GitLab, HackerNews, Google CSE, and custom URLs with 80+ built-in query templates across global regions',
      '100% local AI enrichment via Mistral 7B on Ollama — zero API cost, zero data leaving your server',
      'Two-pass AI pipeline: lead qualification + scoring on first pass, hyper-personalised outreach copy on second pass',
      'Smart deduplication: SHA-256 email fingerprinting + Levenshtein fuzzy match on name + domain',
      'Real-time dashboard with live WebSocket updates — filter by quality, source, status, date, and location',
      'Manual category override (hot/warm/cold/disqualified) and per-lead notes for human-in-the-loop refinement',
      'Daily target mode: set a lead quota (e.g. 500/day), pipeline runs continuously until hit, auto-pauses until midnight',
      'Continuous pipeline with hourly sweep catching any leads Ollama missed during the main run',
      'CSV export for dropping leads into any CRM or outreach tool',
      'Configurable ICP + product description — every future lead scored against your exact criteria',
      'JWT-authenticated API with rate limiting on all endpoints',
      'Zero recurring AI cost — no OpenAI/Claude/SaaS bills; limited only by hardware and scraping rate limits',
    ],
    year: 2025,
  },
  {
    id: 'twitter-ai-bot',
    title: 'Twitter AI Bot — Autonomous Growth Engine',
    shortDescription:
      'A fully automated Twitter presence powered by a local Ollama LLM — generates and posts branded content across 5 weighted content pillars, replies to home feed and search conversations, and scrapes the portfolio site for showcase tweets. Zero Twitter API cost.',
    description:
      'The Twitter AI Bot is a Node.js automation engine that maintains a consistent, on-brand Twitter presence without manual effort. It uses Playwright with the stealth plugin to operate through a browser session (cookie-based auth), bypassing the need for Twitter API credentials entirely. A local Ollama LLM (llama3 by default, configurable) generates all content guided by a personality.json profile and a weighted rotation across five content pillars: backend systems, AI/LLMs, dev productivity, career/remote, and hot takes. Each tweet is also assigned one of five format types — short take, numbered insight, hot take, Gen-Z tone, or CTA — using a weighted random selection to keep the feed varied. A portfolio scraper fetches live content from buildwithkulshresth.com and triggers showcase tweets 20% of the time to surface real project work. Rate limiting enforced via JSON state files caps daily output and prevents aggressive posting patterns. A reply engine handles two engagement flows: home feed replies (contextual responses to people the account follows) and search-based replies (discovery via keyword tracking). Four cron schedules coordinate everything: tweets every hour 8am–10pm IST, home feed replies at 10am, search replies at 2pm and 7pm.',
    techStack: ['Node.js', 'Playwright', 'Ollama', 'node-cron', 'playwright-extra', 'Stealth Plugin'],
    status: 'LIVE',
    architecture: [
      'Browser automation layer — Playwright + playwright-extra + puppeteer-extra-plugin-stealth. Persistent cookies.json session maintained via import-cookies.js. No official Twitter API credentials required; all interactions go through the browser DOM.',
      'AI generation (ollama.js) — Ollama HTTP API at configurable endpoint (default localhost:11434). Weighted pillar selection (backend 25%, AI/LLMs 20%, dev productivity 20%, career 20%, hot takes 15%). Weighted format selection (short take 35%, numbered insight 25%, hot take 20%, Gen-Z 15%, CTA 5%). Prompt constructed from personality.json + pillar + format instruction. Startup health check aborts if Ollama is unreachable.',
      'Posting pipeline (twitter.js) — Playwright navigates to Twitter compose, fills the tweet text, and clicks post. Random 1–4 minute pre-post delay jitter added after generation to avoid predictable timing patterns. 20–30% random skip rate per cron window for organic cadence.',
      'Rate limiting (rateLimit.js) — daily tweet count stored in JSON state file. configurable maxPerDay cap enforced before every post attempt.',
      'Topic tracker (topicTracker.js) — topic bank loaded from topics.json. Tracks which topics have been used in the current cycle; rotates to next cycle when all are exhausted. Cycle count and remaining count logged on each run.',
      'Reply engine (replyBot.js) — runHomeFeedReplySession scans home feed for recent posts from followed accounts and generates contextual replies. runReplySession runs keyword-based search to discover and reply to relevant conversations outside the follow graph.',
      'Portfolio scraper (portfolioScraper.js) — fetches live content from buildwithkulshresth.com, extracts project/blog items, feeds a random item into generateShowcaseTweet for a 20% chance on each posting cycle.',
    ],
    features: [
      'Zero Twitter API cost — entirely browser-driven via Playwright stealth; works without API keys or developer account',
      'Local Ollama LLM for generation — no OpenAI/Claude API bills; all inference runs on local hardware',
      'Five weighted content pillars: backend systems, AI/LLMs, dev productivity, career/remote, hot takes — rotating for variety and reach',
      'Five tweet format types with weighted selection: short take, numbered insight, hot take, Gen-Z tone, CTA — no two consecutive posts feel the same',
      'Portfolio showcase mode: scrapes live site, generates tweets about real projects — 20% activation rate per posting cycle',
      'Reply automation: home feed engagement (10am daily) + search-based discovery replies (2pm and 7pm) — two distinct conversation surfaces covered',
      'Random skip rate (20–30% per window) + 1–4 minute post delay jitter — organic timing that does not look like a bot',
      'Daily rate cap enforced via JSON state file — configurable maxPerDay hard ceiling',
      'Topic cycle tracking: exhausts full topic bank before repeating, logs remaining count and cycle number on every run',
    ],
    year: 2025,
  },
]
